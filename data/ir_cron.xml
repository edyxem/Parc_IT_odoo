<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Cron quotidien : génération des alertes à 8h -->
        <record id="cron_generer_alertes" model="ir.cron">
            <field name="name">IT Parc : Génération alertes quotidienne</field>
            <field name="model_id" ref="model_it_alerte"/>
            <field name="state">code</field>
            <field name="code">
                model._cron_generer_alertes()
            </field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
            <field name="nextcall" eval="(DateTime.now().replace(
                hour=8, minute=0, second=0, microsecond=0
            ) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="priority">5</field>
            <field name="user_id" ref="base.user_root"/>
        </record>

        <!-- Cron quotidien : mise à jour état contrats expirés -->
        <record id="cron_maj_contrats_expires" model="ir.cron">
            <field name="name">IT Parc : Mise à jour contrats expirés</field>
            <field name="model_id" ref="model_it_contrat"/>
            <field name="state">code</field>
            <field name="code">
                model._cron_maj_contrats_expires()
            </field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
            <field name="nextcall" eval="(DateTime.now().replace(
                hour=7, minute=0, second=0, microsecond=0
            ) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="priority">5</field>
            <field name="user_id" ref="base.user_root"/>
        </record>

    </data>
</odoo>